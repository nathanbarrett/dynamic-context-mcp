---
trigger: glob
globs:
  - pages/**/*
---
# Next.js Pages

## Page Conventions
- Use `export default function Page()` for page components.
- `getServerSideProps` or `getStaticProps` for data fetching (Pages Router).
